services:
  db:
    image: mongo:8.2.2
    environment:
      MONGO_INITDB_ROOT_USERNAME: superuser
      MONGO_INITDB_ROOT_PASSWORD: superuser
      MONGO_INITDB_DATABASE: theFashionAppDB
    ports:
      - "27018:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
